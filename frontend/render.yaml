services:
  - type: web
    name: paperpathai-frontend
    env: node
    buildCommand: |
      npm ci
      npm run build
    startCommand: npm run preview -- --host 0.0.0.0 --port $PORT
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: VITE_API_BASE_URL
        value: "https://paperpathai.onrender.com/api"
      - key: VITE_APP_NAME
        value: "PaperPathAI"
      - key: VITE_APP_DESCRIPTION
        value: "Professional student services platform"